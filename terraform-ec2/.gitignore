# Terraform state files
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Variable files (may contain sensitive data)
terraform.tfvars

# SSH keys
*.pem
*.key
